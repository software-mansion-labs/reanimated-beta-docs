import React from "react";
import styles from "./styles.module.css";

const Animation = {
  FADE_IN_OUT: styles.iconClicked,
};

const AnimableIcon = (): JSX.Element => {};

export default AnimableIcon;
