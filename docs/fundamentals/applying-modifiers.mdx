---
sidebar_position: 3
---

import InteractiveExample from "@site/src/components/InteractiveExample";
import ModifierBare from "@site/src/examples/ModifierBare";
import ModifierSpring from "@site/src/examples/ModifierSpring";
import ModifierTiming from "@site/src/examples/ModifierTiming";

# Applying modifiers

## Animating styles

extract your animation logic from your JSX

a very handy `useAnimatedStyle` hook when you'd want to make a bit more complicated

```jsx
const animatedStyles = useAnimatedStyle(() => {
  return {
    transform: [{ translateX: offset.value }],
  };
});
```

The implicit return shorthand format also works:

```jsx
const animatedStyles = useAnimatedStyle(() => ({
  transform: [{ translateX: offset.value }],
})
```

<InteractiveExample name="ModifierBare" component={<ModifierBare />} />

## `withSpring` modifier

<InteractiveExample name="ModifierSpring" component={<ModifierSpring />} />

## `withTiming` modifier

<InteractiveExample name="ModifierTiming" component={<ModifierTiming />} />

## Summary

## What's next?
