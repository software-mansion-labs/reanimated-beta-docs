import InteractiveExample from "@site/src/components/InteractiveExample";
import TimingBasic from "@site/src/examples/TimingBasic";
import ModifierPlayground, {
  useTimingPlayground,
} from "@site/src/components/ModifierPlayground";

# withTiming()

An [animation modifier](/docs/fundamentals/glossary#animation-modifier) based on duration and [easing](https://easings.net/).

<InteractiveExample name="TimingBasic" component={<TimingBasic />} />

## Import

```javascript
import { withTiming } from "react-native-reanimated";
```

## Reference

<details>
<summary><b>Type definitions</b></summary>

```typescript
type AnimatableValue = number | string | number[];

interface WithTimingConfig {
  duration?: number;
  easing?: (value: number) => number;
}

function withTiming<T extends AnimatableValue>(
  toValue: T,
  config?: WithTimingConfig,
  callback?: (finished?: boolean, current?: AnimatableValue) => void
): T;
```

</details>

### Arguments

#### `toValue`

The value on which the animation will come at rest.

#### `config`

The timing animation configuration.

<ModifierPlayground usePlayground={useTimingPlayground} />

Available properties:

| Name                            | Type     | Default                     | Description                                           |
| ------------------------------- | -------- | --------------------------- | ----------------------------------------------------- |
| **duration** <br/> _(optional)_ | `number` | 300                         | Length of the animation (in milliseconds).            |
| **easing** <br/> _(optional)_   | `Easing` | `Easing.inOut(Easing.quad)` | An easing function which defines the animation curve. |

Reanimated provides a handful of ready-to-use easing functions in the `Easing` module.

#### `callback`

A function called on animation complete. In case the animation is cancelled, the callback will receive false as the argument, otherwise it will receive true.

### Returns

An [animation object](/docs/fundamentals/glossary#animation-object). It can be either assigned directly to a [shared value](/docs/fundamentals/glossary#shared-value) or can be used as a value for a style object returned from [useAnimatedStyle](/).

### Easing

The `Easing` module implements common easing functions. This module is used
by `withTiming()` to convey physically
believable motion in animations.

You can find a visualization of some common easing functions at
http://easings.net/

#### Import

```javascript
import { Easing } from "react-native-reanimated";
```

#### Predefined animations

The `Easing` module provides several predefined animations through the
following methods:

- [`back`](docs/easing.html#back) provides a simple animation where the
  object goes slightly back before moving forward
- [`bounce`](docs/easing.html#bounce) provides a bouncing animation
- [`ease`](docs/easing.html#ease) provides a simple inertial animation
- [`elastic`](docs/easing.html#elastic) provides a simple spring interaction

#### Standard functions

Three standard easing functions are provided:

- [`linear`](docs/easing.html#linear)
- [`quad`](docs/easing.html#quad)
- [`cubic`](docs/easing.html#cubic)

The [`poly`](docs/easing.html#poly) function can be used to implement
quartic, quintic, and other higher power functions.

#### Additional functions

Additional mathematical functions are provided by the following methods:

- [`bezier`](docs/easing.html#bezier) provides a cubic bezier curve
- [`circle`](docs/easing.html#circle) provides a circular function
- [`sin`](docs/easing.html#sin) provides a sinusoidal function
- [`exp`](docs/easing.html#exp) provides an exponential function

The following helpers are used to modify other easing functions.

- [`in`](docs/easing.html#in) runs an easing function forwards
- [`inOut`](docs/easing.html#inout) makes any easing function symmetrical
- [`out`](docs/easing.html#out) runs an easing function backwards
